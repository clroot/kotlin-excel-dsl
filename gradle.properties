kotlin.code.style=official
org.gradle.jvmargs=-Xmx2048m
org.gradle.parallel=true

# Project version
version=0.1.0-SNAPSHOT

# Sonatype OSSRH credentials (set in ~/.gradle/gradle.properties or as environment variables)
# ossrhUsername=
# ossrhPassword=

# GPG signing (set in ~/.gradle/gradle.properties or as environment variables)
# signingKey=
# signingPassword=
